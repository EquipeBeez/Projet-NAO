    <form action="{{ path('search_species_result', {'page': 1}) }}" method="POST">
        <div class="input-group input-group-md">
            {{ form_widget(form.fieldsearch) }}
            <div class="input-group-btn">
                <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                <a class="btn btn-default" href="{{ path('view_all_species', {'page': 1}) }}" role="button"><i class="glyphicon glyphicon-arrow-left aria-hidden="true"></i></a>
            </div>
        </div>
    </form>
