    <form action="{{ path('admin_search_user_result', {'page': 1}) }}" method="POST">
        <div class="input-group input-group-md">
            {{ form_widget(form.fieldsearch) }}
            <div class="input-group-btn">
                <button class="btn btn-default" type="submit"><i class="glyphicon glyphicon-search"></i></button>
                <a class="btn btn-default" href="{{ path('admin_users', {'page': 1}) }}" role="button"><i class="glyphicon glyphicon-arrow-left aria-hidden="true"></i></a>
            </div>
        </div>
    </form>
